// ===============================
// Country to language routes
// ===============================
export type Routes = {
    default: string
    [key: string]: string
}

const routes: Routes = {
    default: "en"
}

export default routes;